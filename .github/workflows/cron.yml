name: Scheduled Cron Job

on:
  schedule:
    # Runs every 12 minutes (production only)
    - cron: "*/12 * * * *"
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "production"
        type: choice
        options:
          - production
          - staging

jobs:
  cron:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment: ${{ github.event.inputs.environment || 'production' }}
    concurrency:
      group: cron-${{ github.event.inputs.environment || 'production' }}
      cancel-in-progress: false

    steps:
      - name: Validate required secrets
        env:
          NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          set -euo pipefail

          if [ -z "$NEXT_PUBLIC_APP_URL" ]; then
            echo "NEXT_PUBLIC_APP_URL is missing in GitHub Secrets."
            exit 1
          fi

          if [ -z "$CRON_SECRET" ]; then
            echo "CRON_SECRET is missing in GitHub Secrets."
            exit 1
          fi

          if ! [[ "$NEXT_PUBLIC_APP_URL" =~ ^https?:// ]]; then
            echo "NEXT_PUBLIC_APP_URL must start with http:// or https://."
            exit 1
          fi

      - name: Preflight cron auth/config contract
        env:
          BASE_URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          set -euo pipefail
          NORMALIZED_BASE_URL="${BASE_URL%/}"
          PREFLIGHT_URL="${NORMALIZED_BASE_URL}/api/cron"

          echo "Environment: ${{ github.event.inputs.environment || 'production' }}"
          echo "Preflight URL: ${PREFLIGHT_URL}"

          response_file="$(mktemp)"
          http_status="$(
            curl --silent --show-error \
              --connect-timeout 10 --max-time 30 \
              --retry 2 --retry-all-errors --retry-delay 2 \
              -H "Authorization: Bearer ${CRON_SECRET}" \
              -o "${response_file}" \
              -w "%{http_code}" \
              "${PREFLIGHT_URL}"
          )"

          if [ "${http_status}" != "400" ]; then
            echo "Cron preflight failed. Expected HTTP 400 (invalid job), got HTTP ${http_status}."
            echo "Response body:"
            cat "${response_file}"
            exit 1
          fi

          if ! grep -q '"code":"INVALID_JOB"' "${response_file}"; then
            echo "Cron preflight failed. Expected INVALID_JOB response body."
            echo "Response body:"
            cat "${response_file}"
            exit 1
          fi

          echo "Cron preflight passed."

      - name: Trigger cron endpoint
        env:
          BASE_URL: ${{ secrets.NEXT_PUBLIC_APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          set -euo pipefail
          echo "Environment: ${{ github.event.inputs.environment || 'production' }}"
          NORMALIZED_BASE_URL="${BASE_URL%/}"
          ENDPOINT_URL="${NORMALIZED_BASE_URL}/api/cron?job=HOURLY"
          response_file="$(mktemp)"
          http_status="$(
            curl --silent --show-error -X GET \
              --retry 3 --retry-all-errors --retry-delay 2 \
              --connect-timeout 10 --max-time 60 \
              -H "Authorization: Bearer ${CRON_SECRET}" \
              -o "${response_file}" \
              -w "%{http_code}" \
              "${ENDPOINT_URL}"
          )"

          if [ "${http_status}" != "200" ]; then
            echo "Cron HOURLY execution failed with HTTP ${http_status}."
            echo "Response body:"
            cat "${response_file}"
            exit 1
          fi

          echo "Cron HOURLY execution succeeded."
