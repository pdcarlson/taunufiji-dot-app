/**
 * General Application Constants
 *
 * This file contains static configuration and values that are NOT dependent on the environment.
 * If a value needs to change based on the environment (e.g., staging vs prod), it belongs in .env.
 */

export const CONSTANTS = {
  // Domain/Business Rules
  FINE_AMOUNT_MISSING_DUTY: 50,
  DEFAULT_TASK_LEAD_TIME: 24, // hours

  // other static config can go here...
};
